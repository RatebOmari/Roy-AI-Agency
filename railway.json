{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --include=dev && npm run build && cd backend && npm ci --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/db/migrate.js && (node dist/db/seed.js || echo '[seed] skipped') && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
